# Supabase-Projekt für Login, Karten und Spielstände (siehe supabase/README.md).
# Nach packages/client/.env.local kopieren und ausfüllen. Ohne diese Werte
# läuft das Spiel als reine Gast-Version (kein Login, keine Cloud-Features).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
